(function(){
  'use strict'

  const fieldsets = Array.from( document.querySelectorAll( '.input-fieldset' ) )
  const selectFieldsets = fieldsets.filter( fieldset => fieldset.matches( '.input-fieldset--select' ) )

  selectFieldsets.forEach( fieldset => {
    const radios = fieldset.querySelectorAll(  )
  })
})()